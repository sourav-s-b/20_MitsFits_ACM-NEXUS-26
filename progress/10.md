# NexusPath Progress — 01:35

## React Error Boundary Hardening 
Discovered a critical state mismatch caused by asynchronous backend/frontend server reboots that resulted in an unexpected UI blackout crash.

### 1. Fix: React DOM Fatal Black Screen Crash
**Issue:** When the Python backend was running a stale memory instance (without auto-reload), pulling an old TomTom API definition mapped alternative routes as `[{"latitude": X, "longitude": Y}]` instead of `[{"lat": X, "lon": Y}]`. When this stale data was pushed to the frontend, `App.jsx` mapped `undefined` directly into Leaflet's core SVG engine, triggering a fatal catastrophic failure that entirely crashed the React application layout frame (turning the screen black).
**Solution:** Hardcoded nullish coalescing operators `??` recursively across the `mainRoutePoints` payload parser inside `App.jsx`. The frontend is now fully resilient to data structure drift and legacy cache schemas, parsing legacy data interchangeably without crashing Leaflet natively.

## 01:39

### 2. Fix: Merge Conflict Endpoint Restoration
**Issue:** A `git pull origin sub-main` caused a merge conflict which overwrote `main_routes.py`. The `/shipments`, `/shipments/{id}/history`, and `/login` endpoints essential to the multi-tab layout were completely deleted, causing App.jsx's polling hook to spam `404 Not Found` and blank out the fleet trackers.
**Solution:** Restored all missing REST APIs identically formatted onto the active router block natively.

### 3. Fix: Simulator Loop Connection Blocking
**Issue:** A hard dependency binding the background tracking loop against the isolated 8001 risk engine was blocking iterations inside `simulator.py` if the secondary process wasn't started causing timeout errors and halting truck movement. 
**Solution:** Entirely excised the blocking REST `run_simulation` 8001 call. Shifted back to an instant asynchronous local risk compute model using in-memory parameters logic preventing blocking connection timeouts completely.
